databases:
  # We will use the existing Neon DB, so we don't define a Render DB here.
  # The Neon DB URL will be provided via the Render dashboard as DATABASE_URL.

services:
  - type: web
    name: dsa-platform
    env: docker
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: false
